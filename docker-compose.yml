services:
  specflow-ai:
    build:
      context: .
    container_name: specflow-ai
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
      - ./samples:/app/samples
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    restart: unless-stopped
